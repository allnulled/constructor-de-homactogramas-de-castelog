[ ] Capacidad de `persona.extender.instancia.con.valor(c,v)`
  [ ] que use el `localStorage`
[ ] Capacidad de `persona.extender.clase.con.habilidad(c,v,pxd)`
  [ ] que use el `localStorage`
[ ] Documentarlo en el `README.md`.

